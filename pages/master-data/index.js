import styled from "@emotion/styled";

const Base = styled.div``;

const MasterDataListPage = () => {
  return (
    <Base>
      <h1>MasterDataListPage</h1>
    </Base>
  );
};

export default MasterDataListPage;
